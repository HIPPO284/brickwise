{
  "name": "brickwise-validation-site",
  "version": "0.2.0",
  "private": true,
  "description": "Brickwise AI landing page with a zero-dependency Node.js and SQLite validation backend.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=24"
  }
}
